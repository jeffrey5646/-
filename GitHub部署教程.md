# 📦 管理中台 GitHub 部署教程

## 🎯 目标
将"邻加管理平台"上传到 GitHub，然后通过 Vercel 自动部署

---

## 📋 前提条件

1. ✅ 已安装 Git（检查：在命令行输入 `git --version`）
2. ✅ 有 GitHub 账号（没有的话去 [github.com](https://github.com) 注册）

---

## 🚀 步骤一：创建 GitHub 仓库

### 1. 登录 GitHub
访问 [github.com](https://github.com) 并登录

### 2. 创建新仓库
- 点击右上角的 `+` 号
- 选择 `New repository`（新建仓库）

### 3. 填写仓库信息
```
仓库名称：邻加管理中台
描述：邻加便利店管理平台
可见性：Private（私有）或 Public（公开）
不要勾选：Add a README file
不要勾选：Add .gitignore
不要勾选：Choose a license
```

### 4. 点击 `Create repository`（创建仓库）

---

## 💻 步骤二：准备项目文件

### 1. 打开命令行
在 Windows 上：
- 按 `Win + R`
- 输入 `cmd` 或 `powershell`
- 回车

### 2. 进入项目目录
```bash
cd C:\Users\Administrator\Desktop\线下app\邻加管理平台
```

### 3. 创建 .gitignore 文件
这个文件告诉 Git 哪些文件不需要上传（比如 node_modules）

---

## 📝 步骤三：初始化 Git 并上传

### 1. 初始化 Git 仓库
```bash
git init
```

### 2. 添加所有文件
```bash
git add .
```

### 3. 提交文件
```bash
git commit -m "初始提交：邻加管理平台"
```

### 4. 连接到 GitHub 仓库
```bash
git remote add origin https://github.com/你的用户名/邻加管理中台.git
```

⚠️ **注意**：将 `你的用户名` 替换为你的 GitHub 用户名

### 5. 推送到 GitHub
```bash
git push -u origin main
```

如果提示 `main` 分支不存在，先创建：
```bash
git branch -M main
git push -u origin main
```

### 6. 输入 GitHub 凭据
- 用户名：你的 GitHub 用户名
- 密码：使用 Personal Access Token（不是登录密码）

---

## 🔑 如何获取 Personal Access Token

### 1. 访问 GitHub 设置
- 点击右上角头像
- 选择 `Settings`（设置）

### 2. 进入开发者设置
- 左侧菜单滚动到底部
- 点击 `Developer settings`（开发者设置）

### 3. 创建 Token
- 点击 `Personal access tokens`
- 选择 `Tokens (classic)`
- 点击 `Generate new token`
- 选择 `Generate new token (classic)`

### 4. 配置 Token
```
Note（备注）：邻加管理平台部署
Expiration（过期时间）：No expiration（永不过期）或选择时间
勾选权限：
  ✅ repo（完整的仓库访问权限）
  ✅ workflow（工作流权限）
```

### 5. 生成并复制
- 点击 `Generate token`
- **立即复制 Token**（只显示一次！）
- 保存到安全的地方

### 6. 使用 Token
在 `git push` 时，密码处粘贴这个 Token

---

## ✅ 验证上传成功

### 1. 刷新 GitHub 仓库页面
你应该能看到所有文件已上传

### 2. 检查文件列表
确认以下文件都在：
- ✅ package.json
- ✅ vite.config.ts
- ✅ index.html
- ✅ pages/ 文件夹
- ✅ components/ 文件夹
- ✅ services/ 文件夹

---

## 🎉 完成！

现在你的代码已经在 GitHub 上了，可以进行下一步：通过 Vercel 部署

---

## 🔧 常见问题

### Q1: 提示 "fatal: not a git repository"
**A**: 确保你在正确的目录下，重新运行 `git init`

### Q2: 提示 "Permission denied"
**A**: 
1. 检查 GitHub 用户名是否正确
2. 确保使用的是 Personal Access Token，不是登录密码
3. Token 权限是否包含 `repo`

### Q3: 提示 "failed to push some refs"
**A**: 
```bash
# 先拉取远程代码
git pull origin main --allow-unrelated-histories

# 再推送
git push -u origin main
```

### Q4: node_modules 文件夹太大
**A**: 确保 .gitignore 文件包含 `node_modules/`，然后：
```bash
git rm -r --cached node_modules
git commit -m "移除 node_modules"
git push
```

### Q5: 上传速度很慢
**A**: 
- 检查网络连接
- 考虑使用 GitHub Desktop（图形界面工具）
- 或使用国内的 Gitee 镜像

---

## 📱 使用 GitHub Desktop（可选，更简单）

如果命令行操作不熟悉，可以使用图形界面工具：

### 1. 下载 GitHub Desktop
访问 [desktop.github.com](https://desktop.github.com)

### 2. 安装并登录
使用 GitHub 账号登录

### 3. 添加项目
- File → Add Local Repository
- 选择"邻加管理平台"文件夹
- 点击 "Create Repository"

### 4. 发布到 GitHub
- 点击 "Publish repository"
- 填写仓库名称
- 选择 Private 或 Public
- 点击 "Publish Repository"

### 5. 完成！
所有文件自动上传到 GitHub

---

## 🔄 后续更新代码

当你修改代码后，需要重新上传：

### 使用命令行
```bash
git add .
git commit -m "更新说明"
git push
```

### 使用 GitHub Desktop
1. 打开 GitHub Desktop
2. 查看更改的文件
3. 填写提交说明
4. 点击 "Commit to main"
5. 点击 "Push origin"

---

## 📞 需要帮助？

如果遇到问题：
1. 检查错误信息
2. 参考常见问题部分
3. 搜索错误信息
4. 访问 [GitHub 文档](https://docs.github.com)

---

**下一步**：[部署到 Vercel](./Vercel部署教程.md)
