# 邻加管理平台部署指南

## 📋 目录
- [方案一：Vercel 部署（推荐）](#方案一vercel-部署推荐)
- [方案二：Netlify 部署](#方案二netlify-部署)
- [方案三：自己的服务器](#方案三自己的服务器)
- [方案四：GitHub Pages](#方案四github-pages)
- [方案五：Docker 部署](#方案五docker-部署)

---

## 方案一：Vercel 部署（推荐）

### ✅ 优点
- 完全免费
- 自动 HTTPS
- 全球 CDN 加速
- 自动部署（连接 Git 仓库后）
- 零配置

### 📝 步骤

#### 1. 注册 Vercel 账号
访问 [vercel.com](https://vercel.com) 并使用 GitHub 账号登录

#### 2. 导入项目
- 点击 "Add New Project"
- 选择 "Import Git Repository"
- 选择你的项目仓库

#### 3. 配置环境变量
在 Vercel 项目设置中添加：
```
GEMINI_API_KEY=你的API密钥
```

#### 4. 部署
- 点击 "Deploy"
- 等待构建完成（约 1-2 分钟）
- 获得部署 URL：`https://your-project.vercel.app`

#### 5. 自定义域名（可选）
- 在 Vercel 项目设置中添加自定义域名
- 按照提示配置 DNS 记录

### 🔄 自动部署
每次推送代码到 Git 仓库，Vercel 会自动重新部署

---

## 方案二：Netlify 部署

### ✅ 优点
- 免费
- 简单易用
- 自动 HTTPS
- 表单处理功能

### 📝 步骤

#### 1. 注册 Netlify 账号
访问 [netlify.com](https://netlify.com)

#### 2. 方式 A：拖拽部署（最简单）
```bash
# 1. 构建项目
npm run build

# 2. 访问 Netlify，拖拽 dist 文件夹到页面
```

#### 2. 方式 B：Git 部署（推荐）
- 点击 "Add new site"
- 选择 "Import an existing project"
- 连接 Git 仓库
- 配置构建设置：
  - Build command: `npm run build`
  - Publish directory: `dist`

#### 3. 配置环境变量
在 Netlify 项目设置中添加：
```
GEMINI_API_KEY=你的API密钥
```

#### 4. 部署
点击 "Deploy site"，获得 URL：`https://your-project.netlify.app`

---

## 方案三：自己的服务器

### 📋 前提条件
- 一台服务器（阿里云、腾讯云、AWS 等）
- 已安装 Node.js 18+
- 已安装 Nginx（推荐）

### 📝 步骤

#### 1. 上传代码到服务器
```bash
# 使用 Git
git clone your-repository-url
cd 邻加管理平台

# 或使用 SCP/FTP 上传
```

#### 2. 构建项目
```bash
# 安装依赖
npm install

# 构建
npm run build
```

#### 3. 使用 Nginx 部署

##### 安装 Nginx
```bash
# Ubuntu/Debian
sudo apt update
sudo apt install nginx

# CentOS/RHEL
sudo yum install nginx
```

##### 配置 Nginx
```bash
# 复制配置文件
sudo cp nginx.conf /etc/nginx/sites-available/邻加管理平台
sudo ln -s /etc/nginx/sites-available/邻加管理平台 /etc/nginx/sites-enabled/

# 测试配置
sudo nginx -t

# 重启 Nginx
sudo systemctl restart nginx
```

##### 复制构建文件
```bash
sudo mkdir -p /var/www/邻加管理平台
sudo cp -r dist/* /var/www/邻加管理平台/
```

#### 4. 配置 HTTPS（推荐）
```bash
# 安装 Certbot
sudo apt install certbot python3-certbot-nginx

# 获取证书
sudo certbot --nginx -d your-domain.com

# 自动续期
sudo certbot renew --dry-run
```

#### 5. 访问网站
打开浏览器访问：`http://your-domain.com`

---

## 方案四：GitHub Pages

### ✅ 优点
- 完全免费
- 与 GitHub 集成
- 自动部署

### 📝 步骤

#### 1. 推送代码到 GitHub
```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin your-github-repo-url
git push -u origin main
```

#### 2. 配置 GitHub Actions
已创建 `.github/workflows/deploy.yml` 文件

#### 3. 添加 Secrets
在 GitHub 仓库设置中添加：
- `GEMINI_API_KEY`：你的 API 密钥

#### 4. 启用 GitHub Pages
- 进入仓库 Settings > Pages
- Source 选择 `gh-pages` 分支
- 保存

#### 5. 访问网站
URL：`https://your-username.github.io/your-repo-name/`

### ⚠️ 注意
如果项目不在根路径，需要修改 `vite.config.ts`：
```typescript
export default defineConfig({
  base: '/your-repo-name/',
  // ...
})
```

---

## 方案五：Docker 部署

### ✅ 优点
- 环境一致性
- 易于迁移
- 隔离性好

### 📝 步骤

#### 1. 安装 Docker
```bash
# Ubuntu
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh

# 安装 Docker Compose
sudo apt install docker-compose
```

#### 2. 构建镜像
```bash
docker build -t 邻加管理平台 .
```

#### 3. 运行容器
```bash
# 方式 A：直接运行
docker run -d -p 3000:80 --name 邻加管理平台 邻加管理平台

# 方式 B：使用 Docker Compose
docker-compose up -d
```

#### 4. 访问网站
打开浏览器访问：`http://localhost:3000`

#### 5. 管理容器
```bash
# 查看日志
docker logs 邻加管理平台

# 停止容器
docker stop 邻加管理平台

# 重启容器
docker restart 邻加管理平台

# 删除容器
docker rm 邻加管理平台
```

---

## 🔧 环境变量配置

所有部署方案都需要配置以下环境变量：

| 变量名 | 说明 | 必需 |
|--------|------|------|
| `GEMINI_API_KEY` | Google Gemini API 密钥 | 是 |

### 获取 Gemini API Key
1. 访问 [Google AI Studio](https://makersuite.google.com/app/apikey)
2. 创建 API Key
3. 复制密钥

---

## 📊 性能优化建议

### 1. 启用 Gzip 压缩
已在 Nginx 配置中启用

### 2. 配置缓存策略
静态资源缓存 1 年

### 3. 使用 CDN
- Vercel/Netlify 自带全球 CDN
- 自建服务器可使用 Cloudflare

### 4. 图片优化
- 使用 WebP 格式
- 压缩图片大小
- 使用懒加载

---

## 🔒 安全建议

### 1. HTTPS
所有生产环境必须使用 HTTPS

### 2. 环境变量
不要在代码中硬编码敏感信息

### 3. CORS 配置
根据需要配置跨域策略

### 4. 安全头
已在 Nginx 配置中添加：
- X-Frame-Options
- X-Content-Type-Options
- X-XSS-Protection

---

## 🐛 常见问题

### Q1: 部署后页面空白
**A**: 检查 `vite.config.ts` 中的 `base` 配置是否正确

### Q2: 路由刷新 404
**A**: 确保配置了 SPA 路由重写（已在配置文件中包含）

### Q3: 环境变量不生效
**A**: 
- 检查变量名是否正确
- 重新构建项目
- 清除浏览器缓存

### Q4: 构建失败
**A**:
- 检查 Node.js 版本（需要 18+）
- 删除 `node_modules` 重新安装
- 检查依赖版本兼容性

---

## 📞 技术支持

如有问题，请查看：
- [Vite 文档](https://vitejs.dev/)
- [React 文档](https://react.dev/)
- [Vercel 文档](https://vercel.com/docs)
- [Netlify 文档](https://docs.netlify.com/)

---

## 🎉 推荐方案总结

| 方案 | 难度 | 成本 | 速度 | 推荐度 |
|------|------|------|------|--------|
| Vercel | ⭐ | 免费 | 快 | ⭐⭐⭐⭐⭐ |
| Netlify | ⭐ | 免费 | 快 | ⭐⭐⭐⭐ |
| 自建服务器 | ⭐⭐⭐ | 付费 | 中 | ⭐⭐⭐ |
| GitHub Pages | ⭐⭐ | 免费 | 中 | ⭐⭐⭐ |
| Docker | ⭐⭐⭐ | 付费 | 快 | ⭐⭐⭐⭐ |

**新手推荐**：Vercel 或 Netlify
**企业推荐**：自建服务器 + Docker
**开源项目**：GitHub Pages
